// User Story 1 + 2 scenario checklist for homepage customer areas.
//
// Planned checks once frontend test runner is initialized:
// 1. Render area tiles when API returns items.
// 2. Apply search text and call list endpoint with `search` query.
// 3. Trigger pagination next/previous and call list endpoint with `page` and `pageSize`.
// 4. Show empty-state text when API returns `isEmpty = true`.
// 5. Replace empty-state with tiles after refresh returns non-empty results.
// 6. Show user-facing error message on API failure.
